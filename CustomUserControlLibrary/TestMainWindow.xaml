<Window x:Class="CustomUserControlLibrary.TestMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomUserControlLibrary"
        mc:Ignorable="d"
        Title="TestMainWindow" Height="450" Width="800">
    <Grid>
        <ScrollViewer   HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"  FocusVisualStyle="{x:Null}">
            <Frame x:Name="StackPanelMain" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  FocusVisualStyle="{x:Null}">

            </Frame>
        </ScrollViewer>
    </Grid>
</Window>
